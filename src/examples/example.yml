description: >
  Sample example description.
  # What will this example document?
  # Descriptions should be short, simple, and clear.
usage:
  version: 2.1
  orbs:
    gradle-orb: jdroid/sample-orb@dev:alpha
  jobs:
    build:
      machine: true
      steps:
        - gradle-orb/verify_gradle_wrapper
        - gradle-orb/inspect_cache
        - gradle-orb/clean_up_gradle_cache
